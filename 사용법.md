# 단계별 사용법 (Step-by-step Guide)

이 문서는 이 시스템을 처음 보는 사람이 **그대로 따라 하면 되도록** 썼습니다.

---

## 0. 준비물

| 항목 | 필수 여부 | 없으면 |
|---|---|---|
| 우분투 22.04 (또는 파이썬 3.10+ 리눅스) | 필수 | — |
| USB 웹캠 (UVC) | 화면 시연에 필요 | 영상 파일로 시험 가능 |
| BrainCo Revo2 로봇손 | 로봇을 실제로 움직일 때만 | `--backend none` 으로 화면만 확인 |
| **24V DC 어댑터** | 로봇 사용 시 **필수** | 로봇이 아무 반응도 하지 않습니다 |
| `dialout` 그룹 소속 | 로봇 사용 시 필수 | 포트를 열 수 없습니다 |
| 인터넷 | 최초 설치 때만 | — |

> **가장 흔한 사고:** USB 만 꽂고 24V 어댑터를 안 꽂는 것입니다.
> 이때 포트는 정상적으로 열리고 소프트웨어 지표도 정상이라 원인을 찾기 어렵습니다.

---

## 1. 설치 (최초 1회)

```bash
git clone https://github.com/<계정>/<저장소>.git
cd <저장소>
./설치.sh
```

무엇을 하는지:

1. `python3` 확인
2. `.venv` 라는 **가상환경**을 이 폴더 안에 만듭니다
   → 시스템 파이썬과 `~/.local` 을 전혀 건드리지 않습니다
3. `opencv-python` 과 `mediapipe` 를 그 안에만 설치합니다 (약 200MB)
4. 손 관절 모델 `hand_landmarker.task` (7.8MB) 를 내려받습니다
5. 로봇을 쓸 것인지 물어보고, 원하면 제조사 SDK 도 설치합니다
6. 자체 검증을 돌려 정상인지 보여 줍니다

여러 번 실행해도 안전합니다. 이미 있으면 다시 받지 않습니다.

### 1-1. 시리얼 포트 권한 (로봇을 쓸 때만)

```bash
id -nG | grep dialout          # 나오면 이미 됨
sudo usermod -aG dialout $USER
```

**그룹 변경은 다음 로그인부터 반영됩니다.** 그 전까지는 `실행_웹캠_로봇.sh` 가
자동으로 `sg dialout` 을 써서 처리합니다. 직접 명령을 칠 때도 같은 방법을 씁니다.

```bash
sg dialout -c "./.venv/bin/python src/hand_tracking_control.py --source 0 --backend sdk"
```

---

## 2. 웹캠으로 해 보기 (로봇 없이)

```bash
./실행_웹캠.sh
```

1. 쓸 수 있는 카메라 목록이 먼저 표시됩니다.
2. 창이 하나 열립니다.
3. **손바닥을 카메라 쪽으로 향하게** 하고 화면 안에 넣으십시오(30~60cm).
4. 손 위에 노란 골격이 그려지고, 왼쪽 위에 손가락 6칸의 값(0~1000)이 막대로 표시됩니다.

### 실행 중 키 조작

| 키 | 기능 |
|---|---|
| `q` 또는 `Esc` | 종료 |
| `c` | 가동범위를 표준값으로 초기화 (값이 이상해졌을 때) |
| `space` | 일시정지 / 해제 |
| `o` | 손 펴기(전부 0) |

---

## 3. 로봇을 실제로 움직이기

### 가장 쉬운 방법

```bash
./실행_웹캠_로봇.sh
```

로봇 포트 → 카메라 → SDK → 포트 권한을 차례로 확인한 뒤 창을 띄웁니다.

### 직접 명령으로

```bash
sg dialout -c "./.venv/bin/python src/hand_tracking_control.py --source 0 --backend sdk"
```

재로그인을 한 뒤라면 `sg dialout -c "..."` 없이 그냥 실행해도 됩니다.

### 로봇이 명령대로 움직이는지만 확인

```bash
sg dialout -c "./.venv/bin/python tests/실기_구동시험.py"
```

프리셋 제스처 7종을 차례로 보내고, 매번 **로봇에서 실제 위치를 되읽어 대조**합니다.
"명령을 보냈다"와 "로봇이 움직였다"는 다르기 때문에 이 대조가 필요합니다.

> **주먹에서 엄지가 끝까지 닫히지 않는 것은 정상입니다.** 접힌 손가락에 막히는
> 기구적 한계입니다. `접촉으로 막힌 축` 으로 표시되면 정상입니다.

### ROS 2 경유 (경로 B)

```bash
# 터미널 1 — 컨트롤러 노드
source /opt/ros/humble/setup.bash
source ~/ros2_ws/install/setup.bash
ros2 run ros2_stark_controller stark_node

# 터미널 2 — 손추적
python3 src/hand_tracking_control.py --source 0 --backend ros2
```

> **경고:** 경로 A(SDK)와 경로 B(ROS 2)는 같은 `/dev/ttyUSB0` 을 두고 다툽니다.
> **동시에 켜지 마십시오.** 이전 실행이 남아 있으면 `pkill -f stark_node`.

> **참고:** ROS 2 백엔드는 `rclpy` 와 `ros2_stark_msgs` 가 필요합니다.
> 이들은 시스템 ROS 2 에 있으므로 가상환경 대신 시스템 파이썬으로 실행해야 할 수 있습니다.
> 이 경로는 **미검증**입니다.

---

## 4. 카메라 없이 시험하기

```bash
./.venv/bin/python src/hand_tracking_control.py --source media/시험입력_짧은판.mp4
```

> `media/손추적_시연.mp4` 는 **이미 골격선이 그려진 시연용**이라 입력으로 쓰면 안 됩니다.
> 그림이 손을 가려 검출률이 2%대로 떨어집니다. 검증에는 `시험입력_짧은판.mp4` 를 쓰십시오.

정지 사진 한 장으로도 됩니다.

```bash
./.venv/bin/python src/hand_tracking_control.py --source 어떤사진.jpg
```

---

## 5. 기록을 남기며 실행하기

```bash
./.venv/bin/python src/hand_tracking_control.py \
    --source 0 \
    --backend sdk \
    --headless \
    --record 주석영상.mp4 \
    --csv    기록.csv \
    --readback \
    --max-frames 600
```

| 옵션 | 하는 일 |
|---|---|
| `--headless` | 창을 띄우지 않습니다(원격 접속·서버에서 필요) |
| `--record` | 골격과 수치를 입힌 영상을 저장합니다 |
| `--csv` | 프레임마다 손가락 6칸 값과 원시 각도를 기록합니다 |
| `--readback` | **로봇에서 되읽은 실제 위치**를 CSV 에 함께 기록합니다 |

CSV 는 LibreOffice Calc 로 바로 열립니다(UTF-8 BOM 이라 한글이 깨지지 않습니다).

---

## 6. 전체 검증

```bash
./검증_전체.sh
```

계산 논리 → 제스처 의미 → 영상 처리 → 카메라 → 로봇 실기 구동을 차례로 돌리고
`결과/검증실행_<날짜시각>.log` 에 기록합니다. 로봇이 없으면 5단계만 건너뜁니다.

---

## 7. 손가락 값의 의미

```
인덱스: [ 0     1        2     3     4     5   ]
손가락: [ 엄지  엄지보조  검지  중지  약지  소지 ]
값    :   0 = 완전히 폄   ~   1000 = 완전히 쥠
```

**엄지보조**는 엄지를 손바닥 쪽으로 돌리는 축입니다.
사람 손에는 대응하는 굽힘이 없어 2D 카메라로는 정확히 알 수 없으므로,
기본값에서는 **엄지와 같은 값**을 씁니다. 근거는 `README.md` §6.4 에 있습니다.

---

## 8. 잘 안 될 때

증상별 진단은 [`필독.md`](./필독.md) 에 정리했습니다. 자주 쓰는 것만 추리면:

| 증상 | 조치 |
|---|---|
| 실행 즉시 `AttributeError: ... 'solutions'` | mediapipe 1.x 에서 레거시 API 가 삭제됨. 이 저장소 코드는 이미 Tasks API 사용 |
| 로봇이 무반응 | **24V 어댑터부터** 확인 |
| 포트를 못 연다 | `sudo usermod -aG dialout $USER` 후 재로그인, 또는 `sg dialout -c "..."` |
| 손가락 값이 뒤바뀐다 | 입력 해상도가 낮습니다. `--min-size 640` |
| 값이 덜덜 떨린다 | `--alpha 0.2 --deadband 25` |
| 값이 잘 안 움직인다 | 실행 중 `c` 누르고 손을 활짝 폈다가 꽉 쥐기 |
