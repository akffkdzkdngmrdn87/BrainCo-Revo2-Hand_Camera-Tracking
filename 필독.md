# 필독 — 환경 구성 및 트러블슈팅 (Must Read)

> 이 문서는 카메라 손추적으로 BrainCo Revo2 로봇손을 구동할 때 **실제로 발생한 문제**와
> 그 해소 절차를 **증상 → 원인 → 해결** 3단으로 정리한 것입니다.
> 실행 절차 자체는 [`README.md`](./README.md) 와 [`사용법.md`](./사용법.md) 를 먼저 보십시오.

---

## 0. 문제 발생 시 확인 순서 (Triage Order)

이 순서를 지키면 대부분 3단계 안에서 갈립니다. **소프트웨어를 먼저 의심하지 마십시오.**

| 순서 | 확인 항목 | 확인 명령 |
|:--:|---|---|
| 1 | **24 V 전원 어댑터** 연결 및 LED 점등 | 육안 확인 |
| 2 | USB 장치 노드 존재 | `ls -l /dev/ttyUSB*` |
| 3 | 포트 권한 | `id -nG \| grep dialout` |
| 4 | 포트 선점 프로세스 | `sudo lsof /dev/ttyUSB0` |
| 5 | 파이썬 의존성 | `./.venv/bin/python -c "import mediapipe, cv2, bc_stark_sdk"` |
| 6 | 카메라 | `./.venv/bin/python src/hand_tracking_control.py --list-cameras` |
| 7 | 계산 논리 | `./.venv/bin/python src/hand_tracking_control.py --self-test` |

---

## 1. 프로그램이 실행되자마자 죽는다

### 증상
```
AttributeError: module 'mediapipe' has no attribute 'solutions'
```

### 원인
MediaPipe 레거시 Solutions API(`mp.solutions.hands`)는 **2023-03-01 자로 지원이 종료**되었고,
**mediapipe 1.x 에서는 모듈 자체가 삭제**되었습니다.
검색 상위에 노출되는 예제 다수가 아직 이 API 를 씁니다.

### 해결
이 저장소의 코드는 이미 현행 **Tasks API**(`HandLandmarker`)를 사용합니다.
다른 예제를 참고할 때는 아래를 확인하십시오.

```python
# 낡음 — 지금은 동작하지 않습니다
import mediapipe as mp
hands = mp.solutions.hands.Hands()

# 현행
from mediapipe.tasks import python as mpp
from mediapipe.tasks.python import vision
landmarker = vision.HandLandmarker.create_from_options(...)
```

---

## 2. 모델 파일이 없다고 한다

### 증상
```
[오류] 손 랜드마크 모델 파일을 찾지 못했습니다.
```

### 원인
Tasks API 는 모델 가중치를 라이브러리에 내장하지 않고 `.task` 파일로 분리합니다.
`pip install mediapipe` 만으로는 동작하지 않습니다.

### 해결
```bash
./설치.sh                       # 자동으로 내려받습니다
# 또는 직접
mkdir -p models && curl -L -o models/hand_landmarker.task \
  https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/1/hand_landmarker.task
```

---

## 3. 로봇이 아무 반응도 하지 않는다

### 증상
포트는 열리고 프로그램도 정상 종료되는데 손가락이 전혀 움직이지 않습니다.
또는 연결 단계에서 시간 초과가 납니다.

### 원인
**압도적으로 흔한 원인은 24 V DC 어댑터 미연결입니다.**
USB 는 통신 전용이며 구동 전원을 공급하지 않습니다.
전원이 없어도 USB 시리얼 변환기(FTDI)는 살아 있어 `/dev/ttyUSB0` 이 정상으로 보입니다.
이 때문에 소프트웨어 지표만으로는 판별되지 않습니다.

### 해결
1. **24 V 어댑터를 먼저 확인하십시오.**
2. 그다음 통신을 확인합니다.
   ```bash
   sg dialout -c "./.venv/bin/python tests/실기_구동시험.py"
   ```
   연결되면 장치 정보(S/N·펌웨어·손 방향)가 출력됩니다.

---

## 4. 포트를 열 수 없다 (권한)

### 증상
`/dev/ttyUSB0` 은 보이는데 연결에 실패합니다. 읽기·쓰기 권한이 모두 없습니다.

### 원인
리눅스에서 USB 시리얼 장치는 `dialout` 그룹 소유입니다.
계정이 이 그룹에 없으면 파일이 보여도 열 수 없습니다.

### 해결
```bash
id -nG | grep dialout          # 아무것도 안 나오면 아래 실행
sudo usermod -aG dialout $USER
```

> **그룹 변경은 다음 로그인부터 반영됩니다.** 재로그인 없이 바로 쓰려면
> `sg dialout -c "<명령>"` 으로 감싸십시오.
> `실행_웹캠_로봇.sh` 와 `검증_전체.sh` 는 이 상황을 스스로 판단해 처리합니다.

---

## 5. 카메라가 목록에 없거나, 엉뚱한 장치가 '사용 가능'으로 나온다

### 증상
- `--list-cameras` 에 아무것도 나오지 않는다
- 또는 `/dev/video1` 같은 장치가 '사용 가능'인데 영상이 안 나온다

### 원인
- 전자: 웹캠이 연결되어 있지 않습니다.
- 후자: **OpenCV 는 V4L2 로 열기에 실패하면 조용히 FFMPEG 로 되돌아갑니다.**
  이 때문에 메타데이터 전용 장치까지 '사용 가능'으로 잘못 보고됩니다.
  최근 UVC 웹캠은 영상 노드와 메타데이터 노드를 한 쌍으로 만듭니다
  (예: `video0` = 영상, `video1` = 메타데이터).

### 해결
이 저장소의 코드는 `cv2.CAP_V4L2` 를 명시하고 실제로 한 프레임을 읽어 본 뒤 판정합니다.
직접 코드를 쓸 때도 백엔드를 반드시 지정하십시오.

```python
cap = cv2.VideoCapture(idx, cv2.CAP_V4L2)   # 백엔드를 생략하면 안 됩니다
```

카메라가 없어도 영상 파일로 시험할 수 있습니다.

```bash
./.venv/bin/python src/hand_tracking_control.py --source media/손추적_시연.mp4
```

---

## 6. 손은 검출되는데 손가락 값이 이상하다 (뒤바뀐다)

### 증상
검지를 폈는데 값이 높게(쥠) 나오고, 소지가 낮게(폄) 나오는 등 손가락이 뒤바뀝니다.
오류 메시지는 없습니다.

### 원인
**입력 해상도가 낮으면 MediaPipe 는 검출에 성공하면서도 관절 위치를 틀리게 냅니다.**
25×75 픽셀 그림으로 실측한 결과입니다.

| 입력 | 검지 | 중지 | 약지 | 소지 | 해석 |
|---|--:|--:|--:|--:|---|
| 원본 25×75 | 947 | 878 | 885 | 99 | 완전히 반대 |
| 4배 확대 | 101 | 893 | 952 | 934 | 정답 |

### 해결
`--min-size`(기본 320)가 짧은 변이 그보다 작은 입력을 자동으로 확대합니다.
웹캠(보통 640×480 이상)에서는 발동하지 않습니다. 값을 더 키우려면:

```bash
./.venv/bin/python src/hand_tracking_control.py --source 작은사진.png --min-size 640
```

> 덧붙여, **평면 일러스트는 확대해도 안 됩니다.** MediaPipe 는 실제 손 사진으로
> 학습된 모델이라, 음영도 질감도 없는 벡터 그림에서는 관절 위치를 신뢰할 수 없습니다.

---

## 7. 값이 거의 움직이지 않거나, 반대로 덜덜 떨린다

### 원인과 해결

| 증상 | 조치 |
|---|---|
| 값이 좁은 범위에서만 움직인다 | 실행 중 `c` 를 눌러 가동범위를 초기화한 뒤, 손을 **활짝 폈다가 꽉 쥐어** 범위를 넓히십시오 |
| 값이 덜덜 떨린다 | `--alpha 0.2 --deadband 25` — 더 부드럽게, 더 둔감하게 |
| 반응이 느리다 | `--alpha 0.6 --deadband 6` |
| 로봇이 뚝뚝 끊긴다 | `--rate 15 --duration 180` — 전송을 줄이고 동작 시간을 늘립니다 |

가동범위는 **넓어지기만 하고 좁아지지 않습니다.** 자동 축소를 넣으면 손을 가만히 두었을 때
기준폭이 오그라들어 미세한 떨림이 0~1000 전체로 증폭되기 때문입니다.

---

## 8. 주먹을 쥐어도 로봇 엄지가 끝까지 닫히지 않는다

### 원인
**기구적 한계이며 고장이 아닙니다.** 접힌 네 손가락에 엄지가 물리적으로 막힙니다.
주먹에서는 검지마저 엄지에 막힙니다.

| 조건 | 엄지 실제 도달 (목표 1000) |
|---|--:|
| 네 손가락 편 상태에서 엄지만 쥠 | **989** |
| 같은 조건, 이동 시간 2초 | **990** |
| 같은 조건, 4초 대기 | **988** |
| 네 손가락을 먼저 쥔 뒤 엄지 | 656 |
| 여섯 축 동시에 1000 | 631 |

시간을 늘려도 달라지지 않고, 방해가 없을 때만 989 까지 갑니다.
전류도 200 으로 보호 기준 500 미만이라 과전류 보호가 아닙니다.
사람 손도 주먹을 쥘 때 엄지가 손바닥까지 파고들지 않습니다.

### 확인
```bash
sg dialout -c "./.venv/bin/python tests/실기_구동시험.py"
```
`접촉으로 막힌 축` 으로 표시되면 정상입니다.

---

## 9. 영상 파일로 로봇을 돌렸더니 동작이 뭉개진다

### 원인
영상은 파일에서 읽는 만큼 빠르게 처리됩니다. 40.8초 영상이 16초 만에 끝나기도 합니다.
로봇 손가락이 물리적으로 움직일 시간보다 명령이 빨리 도착합니다.

### 해결
로봇에 연결됐을 때는 자동으로 원래 속도로 늦춥니다(`--pace auto`, 기본값).
강제로 켜거나 끄려면 `--pace on` / `--pace off`.

---

## 10. 종료할 때 이상한 역추적이 뜬다

### 증상 A
```
RuntimeError: Event loop is closed
```
**원인** — 제조사 SDK 는 내부(Rust) 스레드에서 파이썬 asyncio 루프로 콜백을 넣는데,
`asyncio.run()` 이 코루틴이 끝나는 즉시 루프를 닫아 버려 뒤늦은 콜백이 실패합니다.
**해결** — 이 저장소의 코드는 루프를 직접 만들어 실행하고, 끝난 뒤 0.3초 더 돌려
잔여 콜백을 배출한 다음 닫습니다.

### 증상 B
```
TypeError: 'NoneType' object is not callable   (HandLandmarker.__del__ 안에서)
```
**원인** — mediapipe 1.0.1 은 인터프리터 종료 중에 `__del__` 이 돌면 이 예외를 냅니다.
**해결** — 실패 경로를 포함해 항상 먼저 `close()` 를 호출합니다.

두 증상 모두 이 저장소에서는 해소되어 있습니다. 직접 코드를 쓸 때 참고하십시오.

---

## 11. 무해한 경고들

아래는 동작에 영향이 없습니다.

```
QFontDatabase: Cannot find font directory .../cv2/qt/fonts.
INFO: Created TensorFlow Lite XNNPACK delegate for CPU.
W0000 ... Feedback manager requires a model with a single signature inference.
W0000 ... Using NORM_RECT without IMAGE_DIMENSIONS is only supported for the square ROI.
```

`logs/` 폴더가 생기는 것도 정상입니다. 제조사 SDK 가 실행할 때마다 남기는 기록이며,
실행 스크립트가 최근 20개만 남기고 정리합니다.

---

## 12. 그래도 안 될 때 — 상태를 한 번에 보고하기

```bash
./검증_전체.sh
```

`결과/검증실행_<날짜시각>.log` 에 환경·검증 결과·카메라·로봇 상태가 모두 기록됩니다.
이 파일을 첨부하면 진단이 빠릅니다.
